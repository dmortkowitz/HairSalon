<h1>Customer List</h1>
<ul>
  @foreach (var customer in Model)
  {
    <li><a href='/stylists/@Model["customer"].GetStyleId()/customers/@Model["customer"].GetId()'>@customer.GetDescription()</a></li>
  }
</ul>
<a href="/stylists/{styleId}/customers/new">Add a new customer.</a>
